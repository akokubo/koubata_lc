<% provide :title, t("Forgot your password?") %>
<h2><%= yield(:title) %></h2>

<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post, data: { ajax: false } }) do |f| %>
  <%= devise_error_messages! %>

  <div class="ui-field-contain">
    <%= f.label :email %>
    <%= f.email_field :email, autofocus: true %>
  </div>

  <div><%= f.submit t("Send me reset password instructions"), class: "ui-inline" %></div>
<% end %>

<%= render "devise/shared/links" %>
