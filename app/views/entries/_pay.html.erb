<div class="ui-body ui-body-a ui-corner-all">
  <%= form_for(@entry) do |f| %>
    <%= f.label :price %>
    <%= f.number_field :price %>
  	<%= hidden_field_tag :pay, true %>
    <%= f.submit t('Pay') %>
  <% end %>
</div>
